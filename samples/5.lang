// Immutability
//
// Immutable and mutable variables declared with the let and var keywords.
fn main(): void {
    let immutable = 1;
    var mutable = 2;

    while mutable > 2 {
      immutable = 0;
      mutable = mutable - 1;
    }

    return;
}
