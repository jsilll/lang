// Clean Syntax
//
// Unambiguous syntax, modern and easy to parse syntax with elements
// inspired by C, Kotlin, Rust, and Swift.
fn main(): number {
    1;
    println(0, 1);
    return 1;
}

fn println(x: number, y: number): void {
    // This is an empty block
    {
    }
}
