// Lazy Initialization
//
// Data-flow analysis based support for detecting lazily
// initialized and uninitialized variables.
fn dataFlowAnalysis(n: number): void {
    let uninit: number;

    if n > 3 {
        uninit = 1;
    }

    println(uninit);
}

fn println() : void {}
